You are a knowledge and style extraction pipeline. Analyze the conversation log below and
extract three categories of information:

1. Long-term factual memories (extracted_facts): Core facts that are useful to remember indefinitely.
2. User communication style habits (style_habits): Distinct habits, notation choices, emoji choices, or punctuation styles (such as using parentheses for side-thoughts, meta-commentary, or style quirks).
3. Agent behavioral guidelines or rules (behavior_rules): Any specific instructions or rules the user wants the agent to follow (e.g. "always explain code in detail").

EXTRACTION RULES:
1. Extract ONLY explicit, factual statements or clear style/rule habits. Do NOT guess or infer.
2. Write each element as a single, complete, self-contained sentence.
3. Skip generic pleasantries or temporary chat noise.
4. For facts, assign importance_1_to_10.

OUTPUT FORMAT: Valid JSON only. No markdown, no backticks, no explanation.

{
  "extracted_facts": [
    {
      "fact": "User's primary programming language is Rust.",
      "category": "technical",
      "importance_1_to_10": 8
    }
  ],
  "style_habits": [
    "The user frequently uses parentheses (...) to provide meta-insights, self-corrections, or humorous side-notes."
  ],
  "behavior_rules": [
    "Always use standard Markdown formatting for code blocks."
  ]
}

ALLOWED CATEGORIES FOR FACTS: personal | technical | preference | project_state

CONVERSATION LOG:
{CONVERSATION_LOG}
