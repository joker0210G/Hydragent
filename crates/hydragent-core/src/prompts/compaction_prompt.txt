You are a memory curator for an AI agent. The file below is a bounded Markdown memory file that currently exceeds its character limit of {LIMIT} characters.

Your task: Re-synthesize this file to fit within {LIMIT} characters.

RULES:
1. PRESERVE the mandatory header section EXACTLY as-is (everything before the first `#` content section — e.g. the "# User Profile / Name:" block or "# Agent Soul & Personality" block).
2. MERGE similar or redundant entries into single unified statements.
3. RANK by signal: keep the highest-impact facts, preferences, and rules. Drop redundant repetitions.
4. ELIMINATE near-duplicate entries (the file likely contains many paraphrased versions of the same rule — collapse them into one definitive statement).
5. OUTPUT ONLY the new file content — no explanation, no markdown fences, no commentary.
6. The output MUST be strictly fewer than {LIMIT} characters in total.

CURRENT FILE ({CURRENT_LEN} characters, limit is {LIMIT}):
{CONTENT}
