rich>=13.0.0
httpx>=0.25.0
python-dotenv>=1.0.0
python-telegram-bot>=21.0
pyyaml>=6.0
aiohttp>=3.9.0
pyngrok>=7.1.0
graphifyy
graphify
discord.py>=2.3.0
slack-sdk>=3.20.0
# Local SearXNG-compatible shim backed by ddgs (searchxng.py).
# Dux Distributed Global Search — successor to duckduckgo-search.
# Used to provide a SearXNG-shaped /search?format=json endpoint
# without depending on Docker or public SearXNG instances.
ddgs>=9.0.0

