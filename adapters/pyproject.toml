[project]
name = "hydragent-adapters"
version = "0.1.0"
description = "Channel adapters and CLI runner for Hydragent AI agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.0.0",
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
]
